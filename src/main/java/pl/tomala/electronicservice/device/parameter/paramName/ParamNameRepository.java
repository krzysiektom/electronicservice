package pl.tomala.electronicservice.device.parameter.paramName;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ParamNameRepository extends JpaRepository<ParamName, Long> {
}
